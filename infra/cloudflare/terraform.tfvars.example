cloudflare_account_id = "REPLACE_ME"
workers_dev_subdomain = "REPLACE_ME"

# Add these together later when you move to a custom domain:
# cloudflare_zone_id = "REPLACE_ME"
# relay_hostname     = "relay.example.com"
